# Тестовый план — Референт

Основание: [PROJECT.md](./PROJECT.md), [REQUIREMENTS.md](./REQUIREMENTS.md), [PLAN.md](./PLAN.md).

**Окружение:** Windows, dev-сервер `http://localhost:3000`, ключ OpenRouter в `.env.local`.

---

## 1. Сборка и доступность

| ID | Проверка | Ожидание |
|----|----------|----------|
| T-01 | `pnpm.cmd run build` | Exit code 0, без ошибок TypeScript |
| T-02 | GET `/` | HTTP 200 |
| T-03 | CSS `layout.css` | HTTP 200, Tailwind-классы в файле |

---

## 2. Валидация API `/api/parse`

| ID | Вход | Ожидание |
|----|------|----------|
| T-10 | Пустой URL | 400, `{ error: "URL не указан." }` |
| T-11 | `not-a-url` | 400, некорректный URL |
| T-12 | `ftp://example.com` | 400, только http/https |
| T-13 | Wikipedia (EN) | 200, `title` и `content` не пустые |
| T-14 | HTTP 403 (httpbin) | 422, `code: "unavailable"`, дружелюбный текст |
| T-15 | Пустая страница без текста | 422 или 200 с контентом (зависит от URL) |

---

## 3. Валидация API `/api/translate`

| ID | Вход | Ожидание |
|----|------|----------|
| T-20 | Без `article` | 400 |
| T-21 | Неверный `action` | 400 |
| T-22 | `translate` + статья | 200, `result` — Markdown, длина > 500 |
| T-23 | `summary` | 200, есть «О чём статья», длина < 5000 |
| T-24 | `theses` | 200, есть «Тезисы», маркированный список |
| T-25 | `telegram` + `sourceUrl` | 200, есть «Источник:», длина ≤ 1500 |
| T-26 | Ошибка ИИ | 503, `code: "ai_error"`, дружелюбный текст |

---

## 4. Формат ответов (критерии приёмки)

| ID | Действие | Критерий |
|----|----------|----------|
| T-30 | `summary` | Нет полного перевода (короче `translate`) |
| T-31 | `theses` | Раздел «Тезисы», ≥ 3 пункта списка |
| T-32 | `telegram` | Жирный заголовок или первая строка, ссылка на источник |
| T-33 | `translate` | Заголовок `#`, несколько абзацев |

---

## 5. UI (ручная / косвенная)

| ID | Проверка | Ожидание |
|----|----------|----------|
| T-40 | Пустой URL + кнопка | Ошибка под полем ввода |
| T-41 | Недоступная статья | Жёлтый блок в «Результат», не красный под URL |
| T-42 | Ошибка ИИ | Жёлтый блок в «Результат» с понятным текстом |
| T-43 | Бейджи | Перевод / Краткое содержание / Тезисы / Пост для Telegram |

*T-40–T-43 проверяются по логике клиента и API; полный UI — вручную в браузере.*

---

## 6. Прочее

| ID | Проверка | Ожидание |
|----|----------|----------|
| T-50 | POST `/api/process` | 501, сообщение о недоступности |
| T-51 | `.env.local` | Не в git, ключ не в репозитории |

---

## Порядок выполнения

1. T-01 (build)
2. T-02, T-03 (доступность)
3. T-10–T-15 (parse)
4. T-20–T-25 (translate + AI)
5. T-30–T-33 (формат)
6. T-50, T-51

Результаты прогона: [TEST-RESULTS.md](./TEST-RESULTS.md).

## Автоматический прогон

```powershell
cd c:\Work\referent
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-tests.ps1
```

> **Важно:** скрипт вызывает `pnpm.cmd run build`. Перед прогоном остановите dev-сервер (Ctrl+C), иначе T-02/T-03 могут упасть из-за конфликта `.next`.
